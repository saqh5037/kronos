---
title: Kronos — inventario de ramas remotas
date: 2026-09-15
status: reporte del 2026-09-15 (no borró nada) — la limpieza se ejecutó el 2026-09-16, ver el bloque de estado al inicio
baseline: origin/main @ 808fbac (2026-06-10)
---

# Inventario de ramas remotas — 2026-09-15

> ## Estado al 2026-09-16 — qué se ejecutó
>
> La limpieza que este inventario recomendaba **ya corrió**, el 2026-09-16, al
> cerrar la ola de fixes de la fase 0 (ver
> `docs/audit/2026-09-15-kronos-producto/08-fase0-audit.md` §8.9).
>
> **Ejecutado:**
>
> - **11 worktrees `.claude/worktrees/agent-*` removidos.** Los cinco que
>   estaban bloqueados se inspeccionaron antes de quitarlos.
> - **14 ramas `worktree-agent-*` borradas.** Eran scratch de sesiones de
>   agente; tres de ellas eran corrientes de _launch hardening_ de mayo cuyo
>   contenido ya estaba en `main` por otros commits.
> - **11 ramas `rebuild/w0-*` borradas, local y en `origin`**, después de
>   mergear cada una a `rebuild/base`: `admin-mgmt`, `admin-ops`,
>   `atleta-core`, `atleta-infra`, `atleta-leftovers`, `atleta-secondary`,
>   `components`, `hygiene`, `period-summary`, `public-truth`, `wiring`.
>
> **Se conservó a propósito:**
>
> - `backup-pre-rebase-1778364980` — atado a la decisión diferida sobre
>   `feat/ai-programming-cycle` y `feat/admin-crud-editing`. Se borra cuando esa
>   decisión se tome.
> - Los worktrees `../kronos-ai` (`feat/ai-programming-cycle`) y
>   `../kronos-crud` (`feat/admin-crud-editing`), que siguen dados de alta.
>
> **Pendiente, esperando decisión de Samuel:**
>
> - Las tres ramas `feat/*` con PR abierto — **#46**
>   `feat/ai-programming-cycle`, **#45** `feat/admin-crud-editing` y **#42**
>   `feat/super-admin-suite` (que se cierra vía **#43**
>   `feat/super-admin-atletas-pagos`). La decisión se difirió a la fase 3, donde
>   los ciclos de programación ya están calendarizados. Nada se ejecutó de la
>   recomendación de cherry-pick.
> - El resto de las ramas de mayo de la tabla de abajo (`feat/seed-week-simulation`
>   #39, `perf/atleta-fonts-cleanup`, `perf/atleta-streaming` #47 y las de
>   "Delete") siguen exactamente como se levantaron: nadie las tocó.
>
> **Correcciones a las cuentas de este documento** (medidas el 2026-09-16, antes
> de borrar): las ramas `worktree-agent-*` eran **14**, no 9; y las ramas
> `rebuild/w0-*` de la ola 0 eran **11**, no 8 — la ola abrió once worktrees,
> no ocho, y los once se mergearon a `rebuild/base`.
>
> El resto del documento queda tal como se levantó el 2026-09-15: es el
> inventario de ese día, no el estado de hoy.

Levantado durante la limpieza de higiene del repo (`rebuild/w0-hygiene`).
**Este documento no borró ninguna rama, local ni remota.** Las recomendaciones
esperan decisión humana.

`ahead` = commits que la rama tiene y `main` no. `behind` = commits que `main`
tiene y la rama no. Medido con `git rev-list --left-right --count origin/main...<rama>`.

## Resumen

| Recomendación                         | Ramas |
| ------------------------------------- | ----- |
| Merge                                 | 2     |
| Cherry-pick / rebase sobre el rebuild | 5     |
| Delete (contenido ya en `main`)       | 12    |

## Tabla

| Rama remota                          | Ahead | Behind | Último commit | SHA       | PR          | Recomendación                   |
| ------------------------------------ | ----: | -----: | ------------- | --------- | ----------- | ------------------------------- |
| `rebuild/base`                       |     2 |      0 | 2026-09-15    | `d8be40f` | —           | **Merge**                       |
| `audit/2026-09-15-producto`          |     1 |      0 | 2026-09-15    | `9932b09` | #48 abierto | **Merge**                       |
| `feat/ai-programming-cycle`          |     2 |     44 | 2026-05-26    | `dddeba1` | #46 abierto | **Cherry-pick**                 |
| `feat/admin-crud-editing`            |     6 |     44 | 2026-05-26    | `5446a82` | #45 abierto | **Cherry-pick**                 |
| `feat/super-admin-atletas-pagos`     |     6 |     44 | 2026-05-25    | `5c8b55d` | #43 abierto | **Cherry-pick**                 |
| `feat/super-admin-suite`             |     4 |     44 | 2026-05-25    | `3b2874f` | #42 abierto | Cherry-pick vía #43             |
| `feat/seed-week-simulation`          |     2 |     89 | 2026-05-18    | `703b7eb` | #39 abierto | **Cherry-pick** (1 commit útil) |
| `perf/atleta-fonts-cleanup`          |     1 |    158 | 2026-05-10    | `4a1dea7` | sin PR      | **Cherry-pick** (1 commit)      |
| `feat/owner-onboarding-rich`         |     8 |    208 | 2026-05-09    | `58a0ac0` | #10 merged  | Delete (verificar)              |
| `fix/onboarding-bug-bash-2026-05-18` |     1 |     89 | 2026-05-18    | `db9057f` | #38 merged  | Delete                          |
| `feat/wearables-whoop`               |     0 |     83 | 2026-05-19    | `dcdf503` | sin PR      | Delete                          |
| `fix/atleta-prod-readiness`          |     0 |     42 | 2026-05-25    | `d97a0e2` | #44 merged  | Delete (tras cerrar #47)        |
| `perf/atleta-streaming`              |     0 |     34 | 2026-05-26    | `431919b` | #47 abierto | Cerrar #47 + delete             |
| `feat/multi-discipline-foundation`   |     0 |    110 | 2026-05-16    | `a69044a` | #36 merged  | Delete                          |
| `feat/atleta-salud-wellness`         |     0 |    153 | 2026-05-11    | `21cf5ef` | #34 merged  | Delete                          |
| `fix/atleta-otp-paste-feedback`      |     0 |    195 | 2026-05-10    | `4c021b5` | #21 merged  | Delete                          |
| `feat/landing-splitter-atleta-owner` |     0 |    205 | 2026-05-09    | `9b84d64` | #11 merged  | Delete                          |
| `fix/pwa-banner-ios-browsers`        |     0 |    204 | 2026-05-09    | `4e91f22` | #12 merged  | Delete                          |
| `feat/landing-mx-tier-branding`      |     0 |    217 | 2026-05-08    | `e89fb51` | #3 merged   | Delete                          |

## Notas por rama

### Merge

- **`rebuild/base`** — base de la fase 0 del rediseño: `lucide-react`,
  `src/lib/labels.ts` (mapa enum → etiqueta es-MX, con test de completitud) y
  `src/lib/format.ts`. Contiene también el commit de la auditoría, así que
  mergearla cierra `audit/2026-09-15-producto` de paso. Los once worktrees
  `rebuild/w0-*` salen de aquí: es la rama de integración natural.
- **`audit/2026-09-15-producto`** (PR #48) — solo documentación y evidencia
  (`docs/audit/2026-09-15-kronos-producto/`). Sin riesgo de conflicto con
  código. Si se mergea `rebuild/base` primero, #48 queda vacío y se cierra.

### Cherry-pick / rebase

Todas están ~44–158 commits atrás. Mergearlas de frente arrastraría UI vieja
justo contra el rediseño; lo correcto es rebase sobre `rebuild/base` o
cherry-pick del commit útil.

- **`feat/ai-programming-cycle`** (#46, +2) — modelo `ProgrammingCycle` +
  lógica pura de programación con IA y sus tests (~1,183 líneas). Es lógica de
  dominio, sin UI: el candidato más limpio a cherry-pick. Toca
  `prisma/schema.prisma`, así que va **antes** de abrir nuevos worktrees o
  coordinado con la rama base.
- **`feat/admin-crud-editing`** (#45, +6) — editar WOD con movimientos
  anidados, editar atleta, editar clase, acciones de pago/plan en tablas. Cubre
  huecos reales del owner, pero toca justo las pantallas que el rediseño está
  reescribiendo: **rebase, no merge**, y después del sweep de UI.
- **`feat/super-admin-atletas-pagos`** (#43, +6) — superset de #42: chrome de
  super-admin, acceso cross-tenant con código de verificación, vistas de
  atletas y pagos SaaS. Tomar esta rama y descartar #42.
- **`feat/super-admin-suite`** (#42, +4) — contenida en #43. No mergear por
  separado; cerrar el PR cuando #43 aterrice.
- **`feat/seed-week-simulation`** (#39, +2) — de sus dos commits uno
  (`db9057f`) ya está en `main` por otra vía; el útil es `703b7eb`: script
  `seed-week` + e2e de simulación de semana de un box. Cherry-pick de ese
  commit solo. Valioso para el roadmap: los seeds de demo son lo que hace
  presentable la auditoría de pantallas.
- **`perf/atleta-fonts-cleanup`** (+1, sin PR) — un commit que elimina 3 Google
  Fonts y 19 archivos `woff2` sin uso. Ganancia de LCP gratis y riesgo bajo de
  conflicto. **Verificar contra el rediseño primero**: si el sistema nuevo vuelve
  a introducir una fuente, el commit se vuelve obsoleto.

### Delete (contenido ya en `main`)

- **12 ramas con `ahead = 0`**: `feat/wearables-whoop`,
  `fix/atleta-prod-readiness`, `perf/atleta-streaming`,
  `feat/multi-discipline-foundation`, `feat/atleta-salud-wellness`,
  `fix/atleta-otp-paste-feedback`, `feat/landing-splitter-atleta-owner`,
  `fix/pwa-banner-ios-browsers`, `feat/landing-mx-tier-branding`. Cada commit
  está en `main`; borrarlas no pierde nada.
- **`fix/onboarding-bug-bash-2026-05-18`** (+1) — `git cherry` marca su commit
  como equivalente por patch-id a uno que ya está en `main` (PR #38 merged).
  Borrar.
- **`feat/owner-onboarding-rich`** (+8) — PR #10 está **merged**, pero
  `git cherry` marca los 8 commits como `+` porque el merge fue squash y cambió
  los patch-id. Contenido esperado en `main` bajo un commit squasheado (sesión
  90d, password opcional, auto-seed de 50 movimientos, upload de logo, import de
  WODs benchmark, banner de configuración). **Antes de borrar**: confirmar que
  esas 6 features existen en `main` — un `git diff origin/main origin/feat/owner-onboarding-rich -- src/`
  debería salir vacío o solo con ruido de la deriva de 208 commits.
- **`perf/atleta-streaming`** — `ahead = 0` contra `main`, pero su PR #47 sigue
  abierto apuntando a `fix/atleta-prod-readiness` (ya merged). Cerrar #47 y
  borrar ambas ramas.

## Ramas locales sin remoto

No se tocaron y no se subieron. Si no se usan, se borran local sin efecto en
nadie:

- `backup-pre-rebase-1778364980` — snapshot pre-rebase de mayo. Si
  `feat/ai-programming-cycle` y `feat/admin-crud-editing` se resuelven, sobra.
- `feat/audit-ola-1`, `fix/atleta-launch-base`, `fix/atleta-launch-hardening`,
  `polish/atleta-audit-2026-05-10` — trabajo de mayo nunca publicado.
  Revisar si tienen algo que el rediseño quiera antes de borrar.
- 14 ramas `worktree-agent-*` — scratch de sesiones de agente. Desechables.
  (El conteo original de este documento decía 9; el real, medido el 2026-09-16
  antes de borrarlas, era 14.) **Borradas el 2026-09-16.**
- 11 ramas `rebuild/w0-*` — worktrees activos de la fase 0. **No tocar** hasta
  que la integración termine. (El conteo original decía 8; la ola abrió once.)
  **Mergeadas y borradas el 2026-09-16.**

## Cómo se levantó

```bash
git fetch --prune origin
git for-each-ref --format='%(refname:short)|%(committerdate:short)|%(objectname:short)' refs/remotes/origin
git rev-list --left-right --count origin/main...<rama>   # por rama
git cherry -v origin/main <rama>                         # equivalencia por patch-id
gh pr list --state all --limit 60
```
