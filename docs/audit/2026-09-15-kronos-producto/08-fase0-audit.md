---
title: Phase 0 delivery audit (2026-09-16)
status: closed — all P0 and P1 fixed by the 2026-09-16 wave (§8, `f372561` → `1f63ead`); Gate 0 still needs the deploy and Samuel's read of /box
scope: audit run against rebuild/base @ f372561 vs main @ 808fbac (PR #49) — 48 commits, 556 files, +33,786 / −11,232; §8 records the seven-commit fix wave that took the branch to 1f63ead
method: five read-only auditors (public/legal, admin, athlete, cross-cutting hygiene, quality gates) run in parallel on Opus, orchestrated on Fable; every finding carries file:line; the P0 tenancy finding was re-verified by the orchestrator against the source.
inputs: 07-roadmap.md (Phase 0 + Gate 0), CLAUDE.md, docs/branches-2026-09-15.md
---

# Phase 0 delivery audit

Phase 0 promised: nothing the product shows is false, broken or embarrassing. The wave delivered most of the mechanical sweeps (dialect, emoji, tokens, ratchets, service worker, root images, dev routes, athlete runtime errors, period summary wiring) and left three classes of debt: **truth claims that moved from JSX into `src/lib/branding` where no guard looks**, **a tenancy hole that predates the wave but was edited by it**, and **the repository cleanup, which was documented and never executed**.

## 1. Promised vs built

> Sections 1–7 are the audit as it was taken at `f372561` and are left untouched as the historical record. Every status below has since been resolved or reclassified by the fix wave of 2026-09-16 — read **§8** for the current state.

| Phase 0 bullet                                                                         | Status    | Evidence                                                                                                                                                                                                                                                                            |
| -------------------------------------------------------------------------------------- | --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Landing + Terms truth pass, bullets tagged shipped / en piloto / roadmap               | PARTIAL   | Terms §5 is Mercado-Pago-only; roadmap block exists (`(landing)/_components/Pricing.tsx:156-172`). Three shipped-tagged bullets are not backed by code (P0-3) and the Hyrox FAQ is false (P0-3).                                                                                    |
| Remove Stripe, OXXO, SPEI, CFDI, nómina, stores, API, SSO, SLA, 12-week blocks         | REGRESSED | `/box` meta, OG and Twitter descriptions still say "pagos Stripe + Mercado Pago + OXXO" and "LATAM" (`src/lib/branding/index.ts:47,63`). CFDI/stores survive only inside the roadmap block, which is correct.                                                                       |
| One trial length                                                                       | BUILT     | 14 days bound landing ↔ provisioning by `tests/unit/landing-truth.test.ts:184`; two hardcoded literals remain outside the scan (P2).                                                                                                                                                |
| Two CTA labels                                                                         | PARTIAL   | `/box` derives every CTA from two constants; `/atletas` runs a third system (`atletas/_data/copy.ts:14,86`).                                                                                                                                                                        |
| Real seeded numbers, no invented reviews, no scarcity                                  | PARTIAL   | Testimonials deleted. KPIs are invented-but-labelled "DATOS DE EJEMPLO" (`mock.ts:148-168`), not seeded. Scarcity survives in the hero eyebrow "CUPO LIMITADO" (`branding/index.ts:52,68`).                                                                                         |
| One contact address                                                                    | PARTIAL   | `contacto@` still in the founding email; `soporte@` in `AdminErrorState.tsx:61`; the wiring test's regex omits `soporte`.                                                                                                                                                           |
| Remove dev notes and vendor names from owner routes                                    | PARTIAL   | Checkout demo copy is env-gated (correct). "(modo demo)" is written unconditionally into the owner audit log (`src/lib/audit-humanize.ts:70,105`). "OCR Gemini" is shown to athletes (`atleta/programa/page.tsx:145`) and allow-listed by the guard.                                |
| `src/app/dev/*` closed in production                                                   | BUILT     | `dev/layout.tsx:17-21` `notFound()` + `force-dynamic`.                                                                                                                                                                                                                              |
| Dialect sweep + build-failing rule                                                     | PARTIAL   | Delivered as a vitest ratchet, run in CI. 12 baselined hits remain; two are shipped copy ("Esperá", "acá") and five more forms are invisible to the regex (`instalá`, `Tocá`, `Bajá`, `entrá`).                                                                                     |
| Enum → label for every chip                                                            | PARTIAL   | Ratchet at 0 but the rule only sees `>ENUM<` literals. Raw enums rendered via expressions: `Role` on `/eventos/[token]:143`, `WODType` in `OnboardingWizard.tsx:817`, `BookingStatus` in dead `CollapsibleRoster.tsx:114`.                                                          |
| Emoji → Lucide (71 files)                                                              | PARTIAL   | 128 → 25 in 19 files. Emoji regex misses U+2190–21FF, U+2B00–2BFF, U+25A0–25FF.                                                                                                                                                                                                     |
| Unicode arrows → icons                                                                 | MISSING   | 111 arrow/check glyphs in 59 files; no rule covers them.                                                                                                                                                                                                                            |
| A11y floor: `--k-t3`, `<main>`, `MotionConfig`, focus styles                           | PARTIAL   | `MotionConfig reducedMotion="user"` and focus-visible are real. `<main>` exists in both layouts but four athlete pages nest a second `<main>`. `--k-t3 #7a7a84` is 4.71:1 on `--k-bg` and **4.48:1 on `--k-surface`, 4.32:1 on `--k-elevated`**, where most of its 607 usages live. |
| Athlete runtime errors fixed + e2e zero page errors                                    | PARTIAL   | Both fixes are real and unit-tested. `e2e/no-page-errors.spec.ts` covers 8 of 19 athlete routes; `atleta-sweep.spec.ts:203` ends in `expect(true).toBe(true)`; CI runs no Playwright.                                                                                               |
| Landing renders visible on SSR                                                         | BUILT     | Resting state `opacity:1`, entrance is CSS under `prefers-reduced-motion: no-preference`; all landing files import `m`.                                                                                                                                                             |
| Service worker `/uploads`, `/invitacion*` network-only                                 | BUILT     | `public/sw.js:22` v3; `/eventos` and `/tv` still fall through to `networkFirst` (P2).                                                                                                                                                                                               |
| Root images deleted                                                                    | BUILT     | 50 deleted (roadmap said ~200); `.gitignore:70-75` root-anchored.                                                                                                                                                                                                                   |
| Delete `worktree-agent-*` / backup branches                                            | MISSING   | 14 + 1 still local; 11 agent worktrees registered, 5 locked.                                                                                                                                                                                                                        |
| Merge-or-delete decision for 3 feature branches                                        | MISSING   | `docs/branches-2026-09-15.md` records a recommendation; nothing executed; `../kronos-ai` and `../kronos-crud` worktrees still checked out.                                                                                                                                          |
| CLAUDE.md refreshed from the code inventory                                            | STALE     | 8 of 16 spot-checked numbers wrong (test files 160 not 117, `withTenant` 152 not 203, `landing.css` moved, inline styles 2,904 not 2,676, ThemeToggle deleted not "still mounted").                                                                                                 |
| Period summary feeds dashboard, Pagos, Reportes, Atletas (pulled forward from Phase 1) | PARTIAL   | Wiring is real; KPI = table holds by construction on Pagos. Period label absent on `/admin/atletas`. The equality is asserted only in integration tests that CI never runs.                                                                                                         |

Gate 0 criteria: see section 5 (quality gates) for e2e, axe and deploy evidence. "Samuel opens `/box` and finds nothing he would not sign" is blocked by P0-2 and P0-3 below.

## 2. P0 — blocks Gate 0 and the merge

| ID   | Where                                                                                                                                       | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Fix                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | ------------- |
| P0-1 | `src/server/actions/reports.ts:119,126` · `src/server/db.ts:20-90`                                                                          | `db.score.groupBy` and `db.booking.groupBy` run with no `tenantId`. `withTenant()` extends only `findMany/findFirst/findUnique/create/update/delete/count`; `groupBy`, `aggregate`, `upsert`, `createMany`, `updateMany`, `deleteMany`, `findFirstOrThrow`, `findUniqueOrThrow` bypass it. "Top WODs" and "Top atletas por asistencia" on `/admin/reportes` aggregate every box in the database (foreign names print as "—"). Re-verified by the orchestrator. The other four `groupBy` sites (`scores.ts:225,235`, `ai.ts:287`, `feed.ts:97`) and the Whoop `upsert` are bounded by tenant-scoped ids. Pre-existing, but these lines were edited in the wave. | Extend the `$extends` block to every model method (inject `tenantId` into `where` for groupBy/aggregate/updateMany/deleteMany/\*OrThrow, into `data` for createMany, into both for upsert); add `tenantId` to both `where` clauses in `reports.ts`; unit test that enumerates Prisma model methods and fails when one is not intercepted; static guard forbidding `prismaBase.` outside `db.ts` and the auth Box lookup. |
| P0-2 | `src/lib/branding/index.ts:47,63` (rendered by `(landing)/box/page.tsx:37,43,52`)                                                           | `/box` `description`, `openGraph.description` and `twitter.description`: "…White-label real, multi-tenant, pagos Stripe + Mercado Pago + OXXO." plus "LATAM". The first thing Google and an LLM read about the product.                                                                                                                                                                                                                                                                                                                                                                                                                                        | Rewrite both `metaDescription`s to Mercado Pago (tarjeta) y efectivo, México; add `src/lib/branding` to `SCANNED_DIRS` in `landing-truth.test.ts:34-44`.                                                                                                                                                                                                                                                                 |
| P0-3 | `src/lib/branding/index.ts:73-78` (FAQ on `/box?discipline=hyrox`) · `(landing)/_data/mock.ts:71,114-115` · `SectionWhiteLabel.tsx:102,107` | Hyrox FAQ claims a native station editor, splits and per-station leaderboard; the editor is a declared stub (`HyroxWODFormPlaceholder.tsx:30,36`). Pricing bullets (contractual under Terms §3) promise "Subdominio tubox.kronos.app", "dominio propio (app.tubox.mx)", "Cero marca Kronos en … correos" and "Configuración DNS asistida": no `customDomain` field, no host routing in `middleware.ts`, single global email `from` (`src/lib/email.ts:28`).                                                                                                                                                                                                    | Rewrite the Hyrox FAQ to the placeholder's truth; move custom domain, subdomain and own-domain email to the roadmap block; assert in `landing-truth.test.ts` that no `PRICING.features` bullet matches `/dominio propio                                                                                                                                                                                                  | app\.tubox | subdominio/`. |
| P0-4 | `src/app/atleta/reservar/_components/ClassesList.tsx:31-40` (used `:399,527`)                                                               | `toLocaleTimeString` / `toLocaleDateString` without `timeZone` on server-supplied class dates inside a `"use client"` component: SSR renders in the server zone, the phone in its own; wrong class time flashes and hydration mismatches on the main booking screen. Same class in `historial/HistorialView.tsx:251`, `NotificationBell.tsx:276`, `BodyMetricSection.tsx:49`, `ProgramWeekForm.tsx:43`, and in admin `AliasCardList.tsx:126`, `PRChart.tsx:25`, `ScheduleNav.tsx:38`, `AlertRulesPanel.tsx:278`; the dashboard builds its own `Intl.NumberFormat` (`admin/page.tsx:45-51`).                                                                    | Route all of them through `src/lib/format.ts` (pinned `America/Mexico_City`, as `DayContentSection.tsx:23` already does); add a `toLocale` rule to `scripts/guards/rules.ts` over `src/app/**` and `src/components/**`.                                                                                                                                                                                                  |
| P0-5 | `.github/workflows/ci.yml` · `tests/unit/period-summary-period.test.ts:195,230,243` · `tests/unit/atleta-plan-helpers.test.ts:51,60`        | CI `quality` is RED on PR #49 (runs `35038054817`, `35038059140`). Reproduced locally with `TZ=UTC pnpm test`: 6 failures. The "2126/2126" claim holds only in `America/Mexico_City`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Freeze the zone: inject `now` and an explicit `timeZone` in those tests, and set `TZ` in `vitest.config.ts` so local and CI agree.                                                                                                                                                                                                                                                                                       |
| P0-6 | `e2e/**` (27 specs; `git diff main..rebuild/base -- e2e/` = 1 file added, 0 modified)                                                       | Full e2e run against the seeded docker Postgres: 69 pass / 12 real failures / rest cascade. Ten failures are stale copy assertions the wave never updated (`checkout-mp`, `coach-dashboard`, `evento-pasado`, `invite-athletes`, `invite-staff`, `notifications-opt-out`, `reservar`, `saas-checkout`, `wod-date-nav`, `atleta-streak`); `score-pr` times out. Gate 0 says e2e green.                                                                                                                                                                                                                                                                          | Update the specs to the new copy or `data-testid`; isolate `saas-checkout` (may be env); re-run the full suite before merge.                                                                                                                                                                                                                                                                                             |

## 3. P1 — must fix before merge

| ID    | Where                                                                                                                                                                    | What                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Fix                                                                                                                                                                           |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------ | ------------- |
| P1-1  | `src/lib/branding/index.ts:52,68`                                                                                                                                        | Hero eyebrow "PILOTO PRIVADO · MÉXICO · CUPO LIMITADO": the scarcity pattern Phase 0 removed everywhere else.                                                                                                                                                                                                                                                                                                                                                | "PILOTO PRIVADO · MÉXICO"; covered by the P0-2 scan extension.                                                                                                                |
| P1-2  | `tests/unit/landing-truth.test.ts:231-232` · `scripts/guards/rules.ts:42-110`                                                                                            | Truth test's voseo regex uses ASCII `\b` after `á`/`í`, so `acá\b` never matches (verified). Guard `VOSEO_FORMS` lacks `instalá`, `tocá`, `bajá`, `entrá`, `esperá`.                                                                                                                                                                                                                                                                                         | Use `(?!\p{L})` with the `u` flag; add the five forms; then fix the shipped copy below.                                                                                       |
| P1-3  | `(public)/atleta-signup/IosPwaTip.tsx:50,70,84,96,98,100` · `AtletaSignupForm.tsx:171` · `src/server/actions/atleta-photo-wod.ts:74` · `atleta/onboarding/Wizard.tsx:63` | Voseo in shipped copy ("instalá", "Tocá", "Bajá", "entrá", "acá", "Esperá"), "(mejor UX)" jargon, and emoji `📱 💡 ⬆` as icons on the public athlete signup.                                                                                                                                                                                                                                                                                                 | Neutral forms, drop the jargon, Lucide `Smartphone`/`Lightbulb`/`Share`; re-baseline.                                                                                         |
| P1-4  | `src/server/email-templates/founding-reservation.ts:66` · `src/app/admin/_components/AdminErrorState.tsx:61` · `tests/unit/wiring-support-email.test.ts:20`              | Second address `contacto@` in the founding email (the last `PENDING_HANDOFF` entry); `soporte@` in the owner error state; the test regex omits `soporte`.                                                                                                                                                                                                                                                                                                    | `supportMailto()` in both; add `soporte` to the regex; delete `PENDING_HANDOFF`.                                                                                              |
| P1-5  | `src/app/eventos/[token]/page.tsx:143` · `admin/onboarding/OnboardingWizard.tsx:817` · `components/kronos/CollapsibleRoster.tsx:114`                                     | Raw enums rendered through expressions: `{session.user.role}` on a public QR page, `{wod.type}` on the owner's first screen, `{b.status}` in a component with zero importers.                                                                                                                                                                                                                                                                                | `label(...)` / `wodTypeLabel`; delete `CollapsibleRoster.tsx`; extend `raw-enum-jsx` to `{x.(status                                                                           | type       | role)}` children.  |
| P1-6  | `src/app/layout.tsx:11-13,100`                                                                                                                                           | `AchievementToastHost` is commented out while `fireAchievementToast` is called from five live sites (`ScoreForm.tsx:95`, `AthleteScoreForm.tsx:219`, `QuickWodForm.tsx:145,158`, `PhotoWodFlow.tsx:220,233`). Badge unlocks fire into nothing.                                                                                                                                                                                                               | Mount the host (it is built) and assert the toast in `e2e/score-pr.spec.ts`; otherwise delete the five call sites.                                                            |
| P1-7  | `src/app/atleta/programa/page.tsx:145` · `tests/unit/atleta-infra-scan.test.ts`                                                                                          | "OCR Gemini · 1 WOD por foto" shown to athletes and allow-listed by the guard.                                                                                                                                                                                                                                                                                                                                                                               | "Sube una foto de la pizarra · 1 WOD por foto"; drop the allow-list entry; add a vendor-name rule.                                                                            |
| P1-8  | `src/app/atleta/onboarding/steps/Step2PhysicalProfile.tsx:52-70`                                                                                                         | Disabled "Sincronizar con Apple Health · Disponible en iOS app" card with `onClick={() => {}}` and a `// TODO … Ola 2`: promises a native app that does not exist.                                                                                                                                                                                                                                                                                           | Remove the card; add an unshipped-promise rule (`/apple health                                                                                                                | app nativa | disponible en (ios | android)/i`). |
| P1-9  | `src/app/atleta/logros/page.tsx:15` · `logros/[code]/page.tsx:41` · `eventos/page.tsx:20` · `eventos/[slug]/page.tsx:46,129,196`                                         | Page-level `<main>` nested inside the layout's `<main id="main">`: invalid HTML, two landmarks for screen readers.                                                                                                                                                                                                                                                                                                                                           | `<div>`; loop the landmark assertion in `e2e/no-page-errors.spec.ts:125-136` over every athlete route with `main` count `=== 1`.                                              |
| P1-10 | `src/app/globals.css:1643` · `tests/unit/components-guard.test.ts:44`                                                                                                    | `--k-t3 #7a7a84` fails AA on `--k-surface` (4.48:1) and `--k-elevated` (4.32:1); 607 usages, mostly inside `.k-card`. `#54545c` still in `ALLOWED_HEX`.                                                                                                                                                                                                                                                                                                      | `--k-t3: #818189` (≥ 4.5:1 on `--k-elevated`); drop `#54545c`; unit test computing contrast from the token block.                                                             |
| P1-11 | `src/components/admin/ChurnRiskTable.tsx:68` · `src/app/admin/reportes/page.tsx:78,248-250`                                                                              | `{signalCount}/4` with three possible reasons; copy says "No mira el dinero" while rows carry "Membresía vencida sin pagar"; `getChurnRiskList()` ignores the period picker while `getReports` uses it.                                                                                                                                                                                                                                                      | Denominator from the reason union; pass `periodInput`; rewrite the paragraph; unit test pinning both counts to one summary.                                                   |
| P1-12 | `src/lib/audit-humanize.ts:70,105`                                                                                                                                       | "(modo demo)" written unconditionally into the owner audit log.                                                                                                                                                                                                                                                                                                                                                                                              | Drop the parenthetical or gate it dev-only.                                                                                                                                   |
| P1-13 | `scripts/guards/rules.ts:174` · 59 files                                                                                                                                 | Emoji regex misses arrows/geometric ranges; the "unicode arrows → icons" bullet has no enforcement and 111 glyphs remain (`legal/terminos/page.tsx:30`, `OtpRedirectClient.tsx:224`, `Footer.tsx:72`, `ManualScreen.tsx:84`, `PhotoWodFlow.tsx:318`, `invitacion/[token]/page.tsx:29`…).                                                                                                                                                                     | Widen the emoji ranges; add a `unicode-arrow` rule with a seeded baseline; sweep the public + athlete files named by the auditors now, ratchet the rest.                      |
| P1-14 | `.github/workflows/ci.yml:42` · `e2e/atleta-sweep.spec.ts:203` · `vitest.config.ts:10`                                                                                   | CI runs typecheck + lint + `pnpm test` only: no `pnpm build`, no Playwright, no integration tests. The zero-page-error floor and the KPI = table invariant are therefore unenforced; `atleta-sweep.spec.ts` can never go red.                                                                                                                                                                                                                                | Add `pnpm build`; add a Playwright job for `no-page-errors.spec.ts` against the docker Postgres (or state the gap in the PR); delete `atleta-sweep.spec.ts`.                  |
| P1-15 | `CLAUDE.md:26-30,87,102,198-201,235-236`                                                                                                                                 | Numeric claims stale or false (see §1).                                                                                                                                                                                                                                                                                                                                                                                                                      | Regenerate the numeric block from live counts as the last commit before merge; document the third guard `tests/unit/components-guard.test.ts`.                                |
| P1-16 | repo state                                                                                                                                                               | 14 `worktree-agent-*` + `backup-pre-rebase-*` branches, 11 `.claude/worktrees/*` worktrees (5 locked), 9 merged `rebuild/w0-*` branches local and remote.                                                                                                                                                                                                                                                                                                    | `git worktree remove` ×11 (inspect the 5 locked ones first), delete agent/backup/`w0-*` branches local and on origin. The three `feat/*` decisions stay with Samuel (see §6). |
| P1-17 | `e2e/no-page-errors.spec.ts:133`                                                                                                                                         | Order-dependent: fails in a full run (`main#main` count 0 because an earlier spec leaves the seed athlete un-onboarded and the layout redirects), passes 4/4 alone after a reseed. The wave measured it in isolation.                                                                                                                                                                                                                                        | `ensureDemoAthleteOnboarded()` in `beforeEach`, or restore athlete state in the onboarding/signup specs' `afterEach`.                                                         |
| P1-18 | `package.json` · `e2e/`                                                                                                                                                  | Gate 0 "axe re-run on the 14 screens, contrast nodes < 20" is unverifiable: no `@axe-core/playwright`, no script, no spec. The frozen baseline `axe-results.json` holds 325 violation nodes, 187 serious/critical, 186 contrast nodes across `/`, `/box`, `/login`, `/atleta-signup`, `/atleta`, `/atleta/wod`, `/atleta/reservar`, `/atleta/perfil`, `/atleta/skills`, `/admin`, `/admin/atletas`, `/admin/programacion`, `/admin/pagos`, `/admin/ajustes`. | Add `@axe-core/playwright` on `rebuild/base` and an `e2e/axe.spec.ts` over exactly those 14 role/viewport combos writing the same JSON shape; run it after the `--k-t3` fix.  |

## 4. P2 — follow-ups (do not block the merge)

- `src/app/tv/[slug]/page.tsx` — 17 legacy tokens (single worst file), `new Date()` clock rendered once under `revalidate = 30`; `/tv` and `/eventos` cached by the service worker (`public/sw.js:92-101`, bump to v4 with a unit test over the network-only list).
- `src/app/eventos/[token]/page.tsx:59-66,159-164` — no exit link on the not-found and footer states.
- `(landing)/atletas/_data/copy.ts:14,86` — third CTA system; fold into `_data/cta.ts`.
- `SectionOwner.tsx:127` — hardcoded "PROMEDIO · 78 %" while `OWNER_OCCUPANCY` averages 65.7 %.
- `SocialProof.tsx:23` — dead file still carrying "Cupo limitado".
- `src/lib/email.ts:28` / `auth.ts:74` — default `from` `noreply@kronos.app` vs promised `no-reply@kronos-fit.com`.
- `founding-dominus/page.tsx:176`, `founding-reservation.ts:54,71` — hardcoded "14 días".
- `src/lib/scores/copy.ts:68,77` — `rankLabel`/`percentileLabel` unused; views reimplement the guard inline.
- `PersonalizedGreeting.tsx:56,137`, `QuickSurvey.tsx`, `perfil/loading.tsx`, `leaderboard/loading.tsx` — legacy tokens in athlete files.
- `invitacion/[token]/page.tsx:29,90-93`, `AcceptInvitationForm.tsx:51` — `✓` as icon, orphan doc comment; `src/app/invitacion/**`, `wod/foto/**`, `wod/nuevo/**` are in no guard's owned set.
- `src/server/achievements/xp.ts:51,67,80`, `evaluate.ts:90,141,154,237` — `prismaBase` with manual `tenantId`; route through `withTenant` (covered by the P0-1 static guard).
- `components/kronos/TabBar.tsx:22-60` — hand-rolled inline SVG instead of Lucide on the athlete's primary nav.
- `admin/atletas/_components/AthleteDrawer.tsx:61-62` — `new Date()` in render (mitigated by `null` until opened).
- `admin/reportes/page.tsx:177,513-518` — colour switched by score bucket; use `intensity()` from `charts/tokens.ts:82-99`.
- `admin/atletas/page.tsx:252` — hardcoded "14+ días" instead of `AT_RISK_DEFAULT_INACTIVITY_DAYS`; period label missing on the page.
- `tailwind.config.ts:20-22` — `text-text`, `text-text-2`, `text-text-3` utilities map to legacy tokens and are invisible to the `legacy-tokens` guard (`auditoria/page.tsx:188,191`, `ajustes/apodos/page.tsx:42-52`, `AlertRulesPanel.tsx`, `AliasCardList.tsx`).
- `scripts/guards/rules.ts:194` — `raw-enum-jsx` only sees literal children; 87 enum literals exist in JSX files.
- `tests/unit/components-guard.test.ts:26` — dead `EXCLUDED` entry for the deleted `ThemeToggle.tsx`.
- `src/server/email-templates/owner-digest.ts:70` — `✓` glyph outside every guard root.
- Guard roots: UI rules scan only `src/app/**` + `src/components/**`; `src/server/email-templates`, `src/lib`, AI prompts and `.css` are uncovered.

## 5. Quality gates

Run on 2026-09-16 against `rebuild/base @ f372561` with the docker Postgres seeded (`db:seed`, `db:seed:ops`, `db:seed:story`).

| Check            | Result                    | Numbers                                                                           | Notes                                                                                                                                                                                                                                |
| ---------------- | ------------------------- | --------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `pnpm typecheck` | PASS                      | 0 errors, 10 s                                                                    |                                                                                                                                                                                                                                      |
| `pnpm lint`      | PASS with warnings        | 2 warnings, not 3                                                                 | both `no-img-element` in `atleta/wod/foto/PhotoWodFlow.tsx:266,349`; `atleta/ayuda` has no `<img>` any more. `next lint` is deprecated.                                                                                              |
| `pnpm test`      | PASS locally, FAIL in UTC | 160 files · 2126 tests · 4.1 s                                                    | `TZ=UTC` → 6 failed (P0-5). 9 stderr blocks, all deliberate error paths. No test over 5 s.                                                                                                                                           |
| guards `--check` | PASS                      | voseo 12/10 · emoji 25/19 · legacy 28/8 · hex 0 · raw-enum 0                      | equals the committed fixture; no file worsened                                                                                                                                                                                       |
| `pnpm build`     | PASS                      | 40 s · 114 routes · 83 prerendered                                                | 4 warnings: `experimental.typedRoutes` moved, Sentry `sentry.client.config.ts` → `instrumentation-client.ts` deprecation, the 2 lint warnings. Heaviest First Load JS: `/box` 289 kB, `/admin/atletas` 258, `/atletas` 255, `/` 247. |
| e2e              | FAIL                      | 119 tests: 69 pass · 12 real failures · 1 order-dependent · rest cascade · 10 min | P0-6, P1-17. `atleta-sweep.spec.ts` "passes" 15 tests with `expect(true).toBe(true)`.                                                                                                                                                |
| axe              | MISSING                   | baseline 186 contrast nodes vs target < 20                                        | no runner exists (P1-18)                                                                                                                                                                                                             |
| CI on PR #49     | RED                       | `quality` failed twice; `lighthouse` passes                                       | CI runs typecheck + lint + unit only; `lighthouserc.json` sets every assertion to `warn`, so it gates nothing.                                                                                                                       |
| Deploy evidence  | NONE for Phase 0          | 0 tags, 0 releases, 0 GitHub deployments                                          | `scripts/deploy.sh` deploys `main`; `rebuild/base` is 48 commits ahead. Runbook, rollback and smoke script exist. `vercel.json` declares 6 crons while `docs/DEPLOY.md:180` says Vercel cron does not apply (dead config, P2).       |

Landing SSR visibility has no browser assertion: `landing-truth.test.ts` greps five files for `initial="hidden"` and checks `.lp-rise { opacity: 1 }`.

Additional P2 from this run: `eslint.config.mjs` has no `ignores`/dir config, so `e2e/`, `tests/`, `scripts/`, `prisma/` are never linted; `docs/DEPLOY.md:180` contains "acá" outside the voseo guard's `src` root; `CLAUDE.md` "Estado actual" quotes `d8be40f`, 117/1402 tests, 26 specs and the pre-ratchet baseline.

## 6. Decisions that need Samuel

1. **Three feature branches** (`feat/ai-programming-cycle`, `feat/admin-crud-editing`, `feat/super-admin-suite`): `docs/branches-2026-09-15.md` recommends cherry-pick ×2 and closing #42 via #43. Nothing was executed. Execute the recommendation, or defer to Phase 3 where the programming cycles are scheduled.
2. **AchievementToast**: mount it (built, five call sites already fire) or delete the whole path. Mounting is the honest option and needs a visual pass.
3. **Unicode arrows**: sweep all 111 now (59 files, mechanical but wide) or add the rule with a baseline and sweep only the public + athlete files flagged here.
4. **Landing KPIs**: keep "DATOS DE EJEMPLO" (labelled, invented) or replace with figures from `prisma/seed`. The roadmap wording was ambiguous.
5. **CI scope**: add Playwright + integration tests against the docker Postgres (slower PRs, real gate) or keep them manual and say so in the PR.

## 7. Not verified by this audit

- Rendered output at 360/768/1280 and the actual SSR HTML: the auditors were read-only; the visual gate runs after the fixes.
- Whether `hola@` and `contacto@` mailboxes exist.
- Contents of the five locked agent worktrees.
- Semantic defects on admin routes covered only by pattern sweeps (`asistencia`, `programacion`, `reservas`, `wods`, `prs`, `movimientos`, `comunicaciones`, `eventos`, `leaderboards`, `ajustes/*`, `super/*`).

## 8. Fix wave (2026-09-16)

Seven commits on top of `f372561`, all on `rebuild/base` (head `1f63ead`). Every number below was re-measured on that head on 2026-09-16; nothing is carried over from the audit run.

### 8.1 The commits

| Hash      | Scope   | What it did                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `5d1d06f` | tenancy | `withTenant()` is now generated from `TENANT_SCOPED_METHODS` — 17 delegate methods instead of 7, models without `tenantId` skipped on purpose. `/admin/reportes` `groupBy` leak closed. `src/lib/tz.ts` does civil-day math in the box zone; period windows and deadline copy are zone-explicit. Tests run with `process.env.TZ ??= "UTC"` (`tests/setup/timezone.ts`) and assert both zones. `tests/unit/with-tenant-coverage.test.ts` adds a raw-client ratchet. 17 files.                                                                                                                                                                                                                                                                                                                                                                                                         |
| `b579f1d` | public  | `/box` meta/OG/Twitter rewritten, Hyrox FAQ told the truth, pricing bullets for subdomain / custom domain / own-domain email moved into the roadmap block, scarcity removed. `landing-truth.test.ts` now scans `src/lib/branding` and `src/lib/contact.ts` with a Unicode-boundary voseo regex; `VOSEO_FORMS` extended. One support address. Signup stopped promising `kronos-fit.com/<slug>` (that route 404s). `box.md` mirror fixed. 24 files.                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `0b49f9f` | atleta  | Every athlete date through `src/lib/format.ts` (`ClassesList`, historial, `NotificationBell`, `BodyMetric`, `ProgramWeekForm` now anchored on the box civil day). New `to-locale-string` guard rule. `AchievementToastHost` mounted plus e2e. "OCR Gemini" and the Apple Health card removed, with vendor-name and unshipped-promise guard rules behind them. Nested `<main>` fixed and the e2e landmark assertion looped over 18 athlete routes. Coach-cards deterministic fallback. Tab bar and toast on lucide. Athlete copy guards took ownership of `wod/foto`, `wod/nuevo` and `invitacion`. 40 files.                                                                                                                                                                                                                                                                         |
| `773db6a` | admin   | `formatMoney` + `formatDateFull/MonthYear/Decimal` in `format.ts`; `src/lib/dates.ts` rebuilt on `tz.ts` and tested under UTC / CDMX / Tokyo; `to-locale-string` baseline reached `{}`. `OnboardingWizard` labels WOD types; dead `CollapsibleRoster` deleted; `raw-enum-jsx` now catches expression children; `legacy-tokens` catches the Tailwind utility form and the legacy mappings left `tailwind.config.ts`. Churn is `n`/3, period-aware and honestly worded; "(modo demo)" out of the audit log; `AthleteDrawer` clock moved into an effect; Atletas KPIs carry their period; the churn table stacks under `sm`; dashboard hero money stopped wrapping. 44 files.                                                                                                                                                                                                           |
| `1ba2ce5` | hygiene | `--k-t3: #7d7d87` (4.91 / 4.67 / 4.50:1 on `--k-bg` / `--k-surface` / `--k-elevated`) with a contrast-matrix test. Emoji rule widened and a `unicode-glyph` rule added; guard roots now include `src/lib` and `src/server/email-templates`; `legacy-tokens`, `to-locale-string` and `raw-enum-jsx` went to zero baselines. At-risk evaluated as of `min(now, period end)`. `sw.js` v4 with `/eventos` and `/tv` network-only plus `sw-network-only` test. TV live clock, labelled types and tokens. Plan session label map. `AdminDashboardV3` sidebar hidden below `lg` and its grids collapse. `.github/workflows/ci.yml`: `quality` gained `pnpm build`, and a new `e2e` job (nightly 08:00 UTC, `workflow_dispatch`, PR label `run-e2e`) runs Postgres 16 on 5434, seeds, `pnpm test:integration` and `pnpm test:e2e`. `k-class-hooks` test. `docs/DEPLOY.md` dialect. 47 files. |
| `9b12b41` | e2e     | 27 specs repaired against the shipped copy with `data-testid` hooks; `atleta-sweep` now asserts an empty issue list; the `signOut` fixture race fixed (a cookie was leaking across roles); test timeout raised to 90 s. `e2e/axe.spec.ts` + `e2e/fixtures/axe-baseline.json` ratchet the audit's 14 screens, with results in `axe-results-2026-09-16.json` and scripts `test:e2e:axe` / `test:e2e:axe:update`; `@axe-core/playwright` added. `--k-t4` was being used as text in the admin nav headers (1.63:1) → moved to `--k-t3`, and the token is decoration-only from here. `/admin` mounts the responsive `AdminSidebar` below `lg`. `#54545c` gone from the OG images and canvases. `src/lib/week.ts` zone-safe. Landing arrows on lucide. Emoji baseline `{}`. 59 files.                                                                                                      |
| `1f63ead` | atleta  | Past class cards, locked skills, pending nodes and the tab bar dim by **chrome** — surface, border, accent, `Lock` icon, label — instead of container opacity; the tab bar is opaque. `atleta-dimmed-state-guard` test. Axe ratchet reached 0 violation nodes on all 14 screens. 7 files.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

### 8.2 P0 — status after the wave

| ID                                     | Status                             | Evidence                                                                                                                                                                                                                                                                                |
| -------------------------------------- | ---------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P0-1 tenancy `groupBy` leak            | **FIXED** `5d1d06f`                | `TENANT_SCOPED_METHODS` covers 17 methods (`where` ×13, `data`, `dataList` ×2, `upsert`); `tests/unit/with-tenant-coverage.test.ts` enumerates the delegate and fails on an uncovered method, and its raw-client ratchet forbids `prismaBase.` outside `db.ts` and the auth Box lookup. |
| P0-2 `/box` meta claims                | **FIXED** `b579f1d`                | `rg "Stripe\|OXXO\|SPEI\|CFDI\|LATAM" src/lib/branding/index.ts` → no matches. `landing-truth.test.ts` now scans that directory.                                                                                                                                                        |
| P0-3 Hyrox FAQ + pricing bullets       | **FIXED** `b579f1d`                | FAQ rewritten to the placeholder's truth; subdomain, custom domain and own-domain email moved into the roadmap block.                                                                                                                                                                   |
| P0-4 bare `toLocale*` on server dates  | **FIXED** `0b49f9f` + `773db6a`    | `to-locale-string` rule added over `src/app`, `src/components` and `src/server/email-templates`; baseline `{}`.                                                                                                                                                                         |
| P0-5 CI red / tests only green in CDMX | **FIXED** `5d1d06f`                | `tests/setup/timezone.ts` pins `TZ` to UTC unless overridden. Re-measured: 171 files / 2378 tests pass under the default TZ **and** under `TZ=America/Mexico_City`.                                                                                                                     |
| P0-6 e2e failing on stale copy         | **FIXED** `9b12b41`, with a caveat | 27 specs repaired, `data-testid` hooks added, the `signOut` cookie race fixed. The caveat is the dev server, not the specs — see 8.5.                                                                                                                                                   |

### 8.3 P1 — status after the wave

| ID                                             | Status                                                                                                | Evidence                                                                                                                                                                                                                                                                                  |
| ---------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P1-1 scarcity in the hero eyebrow              | **FIXED** `b579f1d`                                                                                   | "CUPO LIMITADO" gone from `src/lib/branding`.                                                                                                                                                                                                                                             |
| P1-2 voseo regex used ASCII `\b`               | **FIXED** `b579f1d`                                                                                   | Unicode-boundary regex `(?!\p{L})` with the `u` flag in both the truth test and `scripts/guards/rules.ts`; `VOSEO_FORMS` extended.                                                                                                                                                        |
| P1-3 voseo + emoji on athlete signup           | **FIXED** `b579f1d`                                                                                   | `IosPwaTip.tsx` and `AtletaSignupForm.tsx` rewritten; the emoji baseline is `{}`.                                                                                                                                                                                                         |
| P1-4 second contact address                    | **FIXED** `b579f1d` (landing/admin) + `1ba2ce5` (email template)                                      | One support address; `tests/unit/wiring-support-email.test.ts` asserts it and scans `src/` for invented addresses.                                                                                                                                                                        |
| P1-5 raw enums through expressions             | **FIXED** `b579f1d` (`eventos/[token]`) + `773db6a` (`OnboardingWizard`, `CollapsibleRoster` deleted) | `raw-enum-jsx` now matches `{x.status\|type\|role}` children; baseline `{}`.                                                                                                                                                                                                              |
| P1-6 `AchievementToastHost` commented out      | **FIXED** `0b49f9f`, mount confirmed `1ba2ce5`                                                        | `src/app/layout.tsx:15,103`.                                                                                                                                                                                                                                                              |
| P1-7 "OCR Gemini" shown to athletes            | **FIXED** `0b49f9f`                                                                                   | Vendor-name guard rule replaces the allow-list entry.                                                                                                                                                                                                                                     |
| P1-8 Apple Health card                         | **FIXED** `0b49f9f`                                                                                   | Card removed; unshipped-promise rule added.                                                                                                                                                                                                                                               |
| P1-9 nested `<main>`                           | **FIXED** `0b49f9f`                                                                                   | `<div>` in the four athlete pages; the e2e landmark assertion loops 18 athlete routes with `main` count `=== 1`.                                                                                                                                                                          |
| P1-10 `--k-t3` fails AA on cards               | **FIXED** `1ba2ce5`                                                                                   | `#7d7d87` = 4.91 / 4.67 / 4.50:1; `tests/unit/atleta-infra-contrast.test.ts` enforces the matrix. `#54545c` removed from `ALLOWED_HEX` and, in `9b12b41`, from the OG images too.                                                                                                         |
| P1-11 churn denominator, copy and period       | **FIXED** `773db6a`                                                                                   | `n`/3 from the reason union, `periodInput` passed through, paragraph rewritten.                                                                                                                                                                                                           |
| P1-12 "(modo demo)" in the audit log           | **FIXED** `773db6a`                                                                                   | `src/lib/audit-humanize.ts`.                                                                                                                                                                                                                                                              |
| P1-13 unicode arrows unenforced                | **FIXED** `1ba2ce5` + `9b12b41`                                                                       | Emoji ranges widened and a `unicode-glyph` rule added. Baseline is 6 hits in one file (`OtpInput.tsx` mask dots, kept on purpose); the landing arrows moved to lucide. **Partial:** 8 `›` (U+203A) glyphs in `src/app` + `src/components` are outside the rule's character set — see 8.6. |
| P1-14 CI runs no build, no Playwright          | **FIXED** `1ba2ce5`                                                                                   | `quality` runs `pnpm build`; the new `e2e` job runs integration + Playwright nightly, on demand, or on a PR labelled `run-e2e`. `atleta-sweep.spec.ts` now asserts an empty issue list instead of `expect(true).toBe(true)`.                                                              |
| P1-15 `CLAUDE.md` numbers stale                | **FIXED** (this batch)                                                                                | Every number in `CLAUDE.md` regenerated from a command run on `1f63ead`.                                                                                                                                                                                                                  |
| P1-16 repo cleanup never executed              | **FIXED** (orchestrator, 2026-09-16)                                                                  | See 8.7.                                                                                                                                                                                                                                                                                  |
| P1-17 `no-page-errors.spec.ts` order-dependent | **FIXED** `9b12b41`                                                                                   | Fixture race fixed (the `signOut` cookie was leaking across roles).                                                                                                                                                                                                                       |
| P1-18 no axe runner                            | **FIXED** `9b12b41`                                                                                   | `@axe-core/playwright`, `e2e/axe.spec.ts`, `e2e/fixtures/axe-baseline.json`, scripts `test:e2e:axe` / `test:e2e:axe:update`, results in `axe-results-2026-09-16.json`.                                                                                                                    |

Nothing in §2 or §3 is left open. What survives is in §4 (P2) plus the new follow-ups in 8.6.

### 8.4 Quality gates after the wave

Same shape as §5. "Before" is the `f372561` run of 2026-09-16; "after" is `1f63ead`, same day.

| Check                   | Before                                                       | After                                                                                                       | Numbers (after)                                                                                                           |
| ----------------------- | ------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| `pnpm typecheck`        | PASS                                                         | **PASS**                                                                                                    | 0 errors                                                                                                                  |
| `pnpm lint`             | PASS, 2 warnings                                             | **PASS, 2 warnings**                                                                                        | both `no-img-element`, both in `src/app/atleta/wod/foto/PhotoWodFlow.tsx`                                                 |
| `pnpm test`             | PASS locally, FAIL in UTC                                    | **PASS in both zones**                                                                                      | 171 files · 2378 tests, green with the default `TZ` and with `TZ=America/Mexico_City`                                     |
| `pnpm test:integration` | never run in CI                                              | **PASS, and CI runs it**                                                                                    | 5 files · 27 tests                                                                                                        |
| guards `--check`        | voseo 12/10 · emoji 25/19 · legacy 28/8 · hex 0 · raw-enum 0 | **5 of 7 rules at zero**                                                                                    | voseo 9/7 · emoji-in-jsx 0 · unicode-glyph 6/1 · legacy-tokens 0 · banned-hex 0 · to-locale-string 0 · raw-enum-jsx 0     |
| `pnpm build`            | PASS, not in CI                                              | **PASS, and CI runs it**                                                                                    | 114 app routes                                                                                                            |
| e2e                     | FAIL — 69 pass · 12 real failures                            | **Green in isolation; the full run degrades**                                                               | 28 specs. Full run: 109 passed · 2 failed · 7 skipped · 4 did not run · 17.2 min                                          |
| axe                     | MISSING — no runner                                          | **PASS, ratcheted**                                                                                         | 0 violation nodes, 0 contrast nodes on all 14 screens                                                                     |
| CI on PR #49            | RED (`quality` failed twice)                                 | **`quality` covers typecheck + lint + test + build**; `e2e` nightly / `workflow_dispatch` / label `run-e2e` | `lighthouserc.json` still sets every assertion to `warn`, so `lighthouse.yml` gates nothing                               |
| Deploy evidence         | NONE                                                         | **still NONE**                                                                                              | `scripts/deploy.sh` deploys `main`; `rebuild/base` is ahead and unpushed (7 commits past `origin/rebuild/base @ f372561`) |

### 8.5 Axe

Fourteen screens, the same role/viewport combos the audit froze: `/`, `/box`, `/login`, `/atleta-signup`, `/atleta`, `/atleta/wod`, `/atleta/reservar`, `/atleta/perfil`, `/atleta/skills`, `/admin`, `/admin/atletas`, `/admin/programacion`, `/admin/pagos`, `/admin/ajustes`.

| Metric             | Audit baseline (`axe-results.json`) | After the wave (`axe-results-2026-09-16.json`) |
| ------------------ | ----------------------------------: | ---------------------------------------------: |
| Violation nodes    |                                 325 |                                          **0** |
| Serious / critical |                                 187 |                                          **0** |
| Contrast nodes     |                                 186 |                                          **0** |

Gate 0 asked for fewer than 20 contrast nodes. Every entry in `e2e/fixtures/axe-baseline.json` is `{"seriousCritical": 0, "contrast": 0}`, so the ratchet now fails on the first regression.

### 8.6 Gate 0, line by line

> _Deploy to prod, axe re-run on the 14 screens (contrast nodes < 20), e2e green, Samuel opens `/box` and finds nothing he would not sign._

| Criterion                | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Deploy to prod           | **NOT DONE — human.** `scripts/deploy.sh` deploys `main`, and `rebuild/base` is ahead of it and not even pushed to `origin` yet. This needs Samuel, not an agent.                                                                                                                                                                                                                                                                                           |
| axe, contrast nodes < 20 | **MET.** 186 → 0, ratcheted in CI's `e2e` job.                                                                                                                                                                                                                                                                                                                                                                                                              |
| e2e green                | **Green in isolation; the full run is degraded by the dev server.** 109 passed / 2 failed / 7 skipped / 4 did not run in 17.2 min. Both failures are `loginAs` 60 s timeouts at tests 117 and 118, caused by one long-lived `next dev` process degrading after roughly 15 minutes; both specs pass in 4–7 s when run alone. That is a harness problem, not a product regression — fix it by running the suite against `next start` or by splitting workers. |
| Samuel opens `/box`      | **PENDING.** The P0s that blocked it (P0-2, P0-3) are fixed and guarded, but the criterion is a human read.                                                                                                                                                                                                                                                                                                                                                 |

### 8.7 Follow-ups opened by this wave

Everything in §4 still stands unless the table above says otherwise. New, all deliberate:

- **9 baselined voseo hits**, in `src/middleware.ts` (2), `src/server/auth.ts` (2), `src/lib/features.ts`, `src/lib/pwa-detect.ts`, `src/lib/disciplines/registry.ts`, `src/lib/disciplines/types.ts`, `src/components/auth/MagicLinkWaiting.tsx`.
- **8 `›` (U+203A) glyphs** in `src/app` + `src/components` that the `unicode-glyph` character set does not cover.
- **Bare `Intl.DateTimeFormat` without `timeZone` is invisible to `to-locale-string`.** One was fixed in `atleta/eventos/[slug]`; the rest is discipline, not a ratchet.
- **`QuickSurvey` renders `opt.emoji`** for its mood options (`src/components/atleta/QuickSurvey.tsx:151,162`). Content decision for Samuel: keep the mood faces or move to icons.
- **`vercel.json` vs `docs/DEPLOY.md`.** `vercel.json` declares 6 crons; `docs/DEPLOY.md` says Vercel cron does not apply. One of the two is dead config.
- **`lighthouserc.json` sets every assertion to `warn`**, so the `lighthouse.yml` workflow gates nothing.
- **`percentileLabel` in `src/lib/scores/copy.ts` is still unused.**
- **`AdminDashboardV3` is a Phase 1 rebuild**, not a Phase 0 fix: it still carries its own internal sidebar, now hidden below `lg`.
- **At-risk KPI semantics.** The window is now honest (`min(now, period end)`), but what "at risk" means is still a product question.
- **Dev-server degradation in long e2e runs** — see 8.6.

### 8.8 Decisions taken, with Samuel's defaults

The five open questions from §6 were resolved with the conservative default in each case:

1. **Three `feat/*` branches** — **deferred**, as `docs/branches-2026-09-15.md` allows: the programming cycles are scheduled for Phase 3. PRs #42, #45 and #46 stay open, and their worktrees (`../kronos-ai`, `../kronos-crud`) stay checked out.
2. **`AchievementToast`** — **mounted.** It was already built with five live call sites; deleting the path would have thrown away working code.
3. **Unicode arrows** — **rule plus a targeted sweep.** `unicode-glyph` was added with a baseline, and the public and athlete files the auditors named were swept now. The remaining `›` are in 8.7.
4. **Landing KPIs** — **kept as labelled examples** ("DATOS DE EJEMPLO"). Replacing them with seed figures is a Phase 1 job, and a labelled example is not a lie.
5. **CI scope** — **build now, e2e by label and nightly.** `quality` gained `pnpm build` on every push; the slow suite runs nightly, on demand, or on a PR labelled `run-e2e`, so a change to booking, auth or tenancy can ask for it.

### 8.9 Repository cleanup executed

P1-16, done on 2026-09-16:

- **11 `.claude/worktrees/agent-*` worktrees removed** (the five locked ones were inspected first).
- **14 `worktree-agent-*` branches deleted.** Three of them were May launch-hardening streams whose content is already in `main` through other commits.
- **11 `rebuild/w0-*` branches deleted**, local and on `origin`, after each one was merged into `rebuild/base`: `admin-mgmt`, `admin-ops`, `atleta-core`, `atleta-infra`, `atleta-leftovers`, `atleta-secondary`, `components`, `hygiene`, `period-summary`, `public-truth`, `wiring`.

Kept on purpose: `backup-pre-rebase-1778364980` (tied to the deferred `feat/ai-programming-cycle` / `feat/admin-crud-editing` decision) and the worktrees `../kronos-ai` and `../kronos-crud`.
