import AtletaPageSkeleton from "@/components/kronos/skeletons/AtletaPageSkeleton";

export default function Loading() {
  return (
    <AtletaPageSkeleton titleWidth={200} subtitleWidth={120} cardCount={2} />
  );
}
